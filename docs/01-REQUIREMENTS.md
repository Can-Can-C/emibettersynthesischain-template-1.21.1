# 开发需求说明书（Requirements）

> 项目：EMI Better Synthesis Chain（NeoForge 1.21.1 / EMI 1.1.24 附属 mod）
> 状态：与用户逐条确认，随实现更新。变更记录见文末。

## 1. 项目概述
本 mod 是 **EMI 附属 mod**：重写 EMI 的配方树（合成树）展示，并新增"悬停即自动合成"。通过 Mixin 注入 EMI 内部类实现定制。

## 2. 功能需求

### 需求 1：配方树按钮行为
- **目标**：点左下角"配方树"按钮 → **不再弹独立 BoM 界面**，切换侧边栏到"收藏页"显示合成树。
- 添加配方树（配方页按钮 / EMI keybind）时页面**打开则保持，未打开则打开**；左下角按钮为开关。
- **验收**：无全屏树窗口；收藏页显示合成树；添加不关闭页面。

### 需求 2：合成树布局
- 布局（用户示例为验收基准）：
  ```
  [栅栏×3] │ [原木×2]                ← 最深（基础）
           │ [木板×6]
           │ [木板×4] [木棍×2]        ← 目标配方直接输入
  ─────────┴───────────────
  副产物区： [木板×2] [木棍×2]
  ```
- 最终产物在**左**（竖分割线左侧），材料区在右**从上到下分行横排**（最深→最浅，最后一行=直接输入）。
- **纯图标 + 右下角数量**，无文字、无箭头；目标与材料间**无连线**（已解析的标签用**短连线**连到右侧"所选成员"图标）；悬停图标显示物品栏样式 tooltip（不显示名字，解决长名截断）。
- 多棵树**纵向叠加**；**副产物**（配方批量合成的过量产出）显示在树底部。
- **验收**：栅栏示例数字（栅栏×3、木板×6、木棍×2、原木×2、副产物木板×2+木棍×2）正确。

### 需求 3：多物品合成树（添加 / 删除 / 选择）
- **添加**：配方页"树"按钮（绿=已在树中，点击切换加入/删除）；EMI keybind/树按钮加入悬停/查看物品。**最后一步配方不需默认**；中间材料按 EMI"设为默认配方"拆解。
- **多材料兼容（标签）**：标签材料显示**标签徽记**；未选择具体材料时**不拆解**；点击标签 → 进入可选择具体材料的界面（EMI 原生 ctrl+单击，选择后自动刷新树）。已解析标签在**右侧用短连线连到所选成员**（成员占正常格子，可点击查配方/自动合成）。
- **分解类配方**（铁块→铁锭等）**不自动选用**，避免循环；取消默认的配方不再拆解。
- **组件感知**：同 id 不同组件（不同时长药水等）各自单独开树；持久化保留组件。
- **删除**：右键点击某树最终产物列 → 删除该树。
- **滚动**：惯性 + 可拖动滚动条 + 滚到底钳制 + 仅鼠标在树区时滚动 + 添加后自动滚到底。
- **验收**：添加/删除/持久化（重启仍在）；标签选择后树刷新；铁块/药水正确；滚动符合上述。

### 需求 4：流体显示
- 流体合成（如黑曜石=岩浆+水）正常显示，**消耗量从配方获取**并以 mB 文本标注在流体图标下。

### 需求 5：自动合成（Phase 3）
- **操作**：打开物品栏 → 备好材料 → 鼠标悬停最终产物（或树节点）→ 按快捷键 → 自动合成。**V=合成一次（得最终结果即停）；Shift+V=连续合成（一直合到材料用完）**。
- **范围**：**仅工作台配方**（`CraftingRecipe`）；烧炼/锻造/模组机器等一律不自动合成；**中间步骤递归自动合成**（缺什么先用工作台合成，含标签成员，深度≤6，循环直至材料齐或无法推进）。
- **规则**：每次 1 个；**模拟玩家合成**（材料摆进合成格，按原版逻辑取结果，余料/容器如桶返还背包）；**打开工作台/背包界面时逐步显示合成过程**（每一步材料进格→显示→取结果→下一步，像鼠标宏不移动鼠标）；**默认配方优先**（客户端 EMI BoM 默认配方映射随请求发，服务端优先使用）；2×2 随时可合，3×3 要求**打开工作台界面**（`containerMenu` 为 `CraftingMenu`，中间 3×3 产物同样要求）；**无法合成时红字提示**（未悬停物品 / 非工作台配方 / 3×3 未打开工作台界面 / 材料不足）；服务端**权威校验**（防作弊）；**标签材料按 tag 匹配**。
- **验收**：2×2（木棍）成功；3×3（钻石镐）打开工作台界面成功/未打开则拒绝；非工作台配方不合成；**完整链条**（红色玻璃板：先合玻璃板+红色染料，再合红色玻璃板）一次 V 完成。

## 3. 非功能需求
- 自动合成需**服务端安装本 mod**（客户端给提示）。
- 小步推进，每阶段独立验证（`docs/03`）。
- 依赖 EMI **1.1.24 本地 jar**；NeoForge 范围 `[21.0.0,)`（可跑 21.1.x 任意补丁）。

## 4. 配置项（可在 **EMI 设置页**的"EMI Better Synthesis Chain"分组里修改，改动即落盘 `config/emibettersynthesischain-common.toml`）
| 键 | 默认 | 说明 |
|----|------|------|
| `treeSidebarWidth` | 10 | 树模式左侧栏物品列数（宽） |
| `treeRowSpacing` | 16 | 合成树行高（px） |
| `treeItemGap` | 4 | 同行物品间距（px） |
| `treeTreeGap` | 8 | 树间垂直间距（px） |
| `treeByproductGap` | 6 | 副产物区上方间距（px） |
| `treeRedMarking` | true | 悬停树时材料不足节点是否标红 |
| `autoCraftShowTicks` | 6 | 可见合成链每步显示时长（tick） |
| `autoCraftGapTicks` | 2 | 可见合成链步间间隔（tick） |
| `autoCraftFailureMessages` | true | 无法合成时是否红字提示 |

快捷键键位：`V`（自动合成，可在按键设置里改绑）。

---

## 变更记录
| 日期 | 变更 |
|------|------|
| 2026-08-08 | 初版：收藏页内显示、可调宽、专用快捷键、3×3 需工作台、每次 1 个、仅工作台配方 |
| 2026-08-09 | 布局改为"最终产物左 + 材料分行横排 + 副产物区"（按用户示例）；纯图标+右下角数量+tooltip；标签徽记+选择；分解类配方不自动选用；组件感知与持久化；流体 mB 用量；惯性滚动+滚动条+自动滚底；点穿/悬停重叠修复；NeoForge 范围放宽 |
| 2026-08-09 | 自动合成升级为**完整链条递归**：循环确保材料（数量感知，缺多少合多少批），先合中间产物（含标签成员）再合目标，中间 3×3 产物同样需工作台 |
| 2026-08-09 | 合成方式改为**模拟玩家合成**（材料摆进 2×2/3×3 合成格，assemble 取结果，余料/容器返还，像鼠标宏不移动鼠标）；**默认配方优先**（客户端 BoM 默认映射随请求发）；3×3 改判**打开工作台界面**（containerMenu=CraftingMenu），移除 autoCraftWorkbenchRadius |
| 2026-08-09 | 自动合成改为**可见合成链逐步显示**（打开工作台/背包时，每一步材料进格→显示→取结果→下一步，全程可见）；**V=合一次得最终结果即停、Shift+V=连续合成**（一直合到材料用完）；加快速度（每步约 6+2 tick） |
| 2026-08-09 | **无法合成时红字提示**（未悬停物品/非工作台配方/3×3 未打开工作台界面/材料不足）；**调小树间距**（PAD/LEFT_COL/ROW_H/ITEM_GAP/TREE_GAP/BP_GAP 更紧凑） |
| 2026-08-09 | **EMI 设置页注入设置**（参考 emi-plus-plus-2）：树行高/间距/标红、自动合成速度/失败提示 9 项可在 EMI 设置页改（`ConfigScreenMixin`，改动即落盘）+ 一键重置按钮；版本定为 **v1.0.0 正式发布** |
